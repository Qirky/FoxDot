# `Env`

## Classes

### `EnvGen(self, value=None)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, value=None)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `env(self, sus=None, amp=None, curve='lin', doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, sus=None, amp=None, curve='lin', doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `linen(self, atk=0.01, sus=None, amp=None, curve=0, doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, atk=0.01, sus=None, amp=None, curve=0, doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `mask(self, sus=None, amp=None, curve='lin', doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, sus=None, amp=None, curve='lin', doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `perc(self, atk=0.01, sus=None, amp=None, curve=0, doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, atk=0.01, sus=None, amp=None, curve=0, doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `ramp(self, sus=None, amp=[1, 1], curve='step', doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, sus=None, amp=[1, 1], curve='step', doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `reverse(self, sus=None, amp=None, curve='exp', doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, sus=None, amp=None, curve='exp', doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

### `sine(self, dur=sus, amp=amp, doneAction=0)`



#### Methods

##### `__attr__(self)`

Converts the attr dict to SCLang arguments 

##### `__call__(self, *args, **kwargs)`

Call self as a function.

##### `__init__(self, dur=sus, amp=amp, doneAction=0)`

Initialize self.  See help(type(self)) for accurate signature.

##### `__repr__(self)`

Return repr(self).

##### `__str__(self)`

Return str(self).

---

## Functions

## Data

